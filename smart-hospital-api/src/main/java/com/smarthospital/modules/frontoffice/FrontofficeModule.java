package com.smarthospital.modules.frontoffice;

/**
 * Module: Front Office (Appointments, enquiries, visitors)
 *
 * Scaffold placeholder — implement per Section 4.2 pattern:
 *   domain/     JPA entities extending AuditEntity
 *   dto/        records for requests and responses
 *   repository/ Spring Data JPA interfaces
 *   service/    @Service @Transactional business logic
 *   controller/ @RestController mapped to /api/v1/frontoffice
 */
public final class FrontofficeModule {
    private FrontofficeModule() {}
}
