spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false   # Testcontainers handles schema; Flyway migrations run in integration tests explicitly

app:
  jwt:
    secret: test-secret-must-be-at-least-32-characters!!

logging:
  level:
    com.smarthospital: DEBUG
