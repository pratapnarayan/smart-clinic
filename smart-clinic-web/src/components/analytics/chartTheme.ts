// Re-export from canonical theme location for backward compatibility
export {
  chartColors,
  chartPalette,
  baseChartOptions,
  chartConfigs,
  formatCurrency,
  formatCompactCurrency,
  formatNumber,
  formatPercentage,
} from '@/theme/chartTheme'
